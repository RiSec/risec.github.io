version: 2.1

# workflows:
#   version: 2
#   workflow:
#     jobs:
#     - deploy:
#         filters:
#           branches:
#             only: develop

jobs:
  build:
    docker:
    - image: circleci/node:10.12.0
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Build
          command: npm run build
      - deploy:
          name: Deploy
          command: npm run deploy
